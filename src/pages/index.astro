---
import BaseLayout from '../layouts/BaseLayout.astro';
import ReviewCarousel from '../components/ReviewCarousel.astro';

const schema = {
  "@context": "https://schema.org",
  "@type": "LocalBusiness",
  "name": "Perfect Pools",
  "description": "Professional pool cleaning, maintenance, and repair services in Richmond Hill, Savannah, and the Greater Savannah area.",
  "url": "https://perfectpoolscleaning.com",
  "telephone": "(912) 459-0160",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "8989 Ford Ave",
    "addressLocality": "Richmond Hill",
    "addressRegion": "GA",
    "postalCode": "31324",
    "addressCountry": "US"
  },
  "openingHoursSpecification": {
    "@type": "OpeningHoursSpecification",
    "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"],
    "opens": "08:00",
    "closes": "17:00"
  },
  "aggregateRating": {
    "@type": "AggregateRating",
    "ratingValue": "4.9",
    "reviewCount": "127"
  }
};

/* Inline SVG icons (Lucide-style, 24x24 viewBox) */
const icons = {
  star: '<svg viewBox="0 0 24 24" fill="currentColor" stroke="none" class="icon"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>',
  droplets: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon"><path d="M7 16.3c2.2 0 4-1.83 4-4.05 0-1.16-.57-2.26-1.71-3.19S7.29 6.75 7 5.3c-.29 1.45-1.14 2.84-2.29 3.76S3 11.1 3 12.25c0 2.22 1.8 4.05 4 4.05z"/><path d="M12.56 6.6A10.97 10.97 0 0 0 14 3.02c.5 2.5 2 4.9 4 6.5s3 3.5 3 5.5a6.98 6.98 0 0 1-11.91 4.97"/></svg>',
  alertCircle: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>',
  arrowRight: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>',
  shield: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon"><path d="M20 13c0 5-3.5 7.5-7.66 8.95a1 1 0 0 1-.67-.01C7.5 20.5 4 18 4 13V6a1 1 0 0 1 1-1c2 0 4.5-1.2 6.24-2.72a1.17 1.17 0 0 1 1.52 0C14.51 3.81 17 5 19 5a1 1 0 0 1 1 1z"/></svg>',
  clock: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>',
  testTube: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon"><path d="M14.5 2v17.5c0 1.4-1.1 2.5-2.5 2.5s-2.5-1.1-2.5-2.5V2"/><path d="M8.5 2h7"/><path d="M14.5 16h-5"/></svg>',
  eye: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon"><path d="M2.062 12.348a1 1 0 0 1 0-.696 10.75 10.75 0 0 1 19.876 0 1 1 0 0 1 0 .696 10.75 10.75 0 0 1-19.876 0"/><circle cx="12" cy="12" r="3"/></svg>',
  home: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon"><path d="M15 21v-8a1 1 0 0 0-1-1h-4a1 1 0 0 0-1 1v8"/><path d="M3 10a2 2 0 0 1 .709-1.528l7-5.999a2 2 0 0 1 2.582 0l7 5.999A2 2 0 0 1 21 10v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/></svg>',
  building: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon"><rect width="16" height="20" x="4" y="2" rx="2" ry="2"/><path d="M9 22v-4h6v4"/><path d="M8 6h.01"/><path d="M16 6h.01"/><path d="M12 6h.01"/><path d="M12 10h.01"/><path d="M12 14h.01"/><path d="M16 10h.01"/><path d="M16 14h.01"/><path d="M8 10h.01"/><path d="M8 14h.01"/></svg>',
  thermometer: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon"><path d="M14 4v10.54a4 4 0 1 1-4 0V4a2 2 0 0 1 4 0Z"/></svg>',
  settings: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon"><path d="M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.22.38a2 2 0 0 0 .73 2.73l.15.1a2 2 0 0 1 1 1.72v.51a2 2 0 0 1-1 1.74l-.15.09a2 2 0 0 0-.73 2.73l.22.38a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.22-.39a2 2 0 0 0-.73-2.73l-.15-.08a2 2 0 0 1-1-1.74v-.5a2 2 0 0 1 1-1.74l.15-.09a2 2 0 0 0 .73-2.73l-.22-.38a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2z"/><circle cx="12" cy="12" r="3"/></svg>',
  cloudRain: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon"><path d="M4 14.899A7 7 0 1 1 15.71 8h1.79a4.5 4.5 0 0 1 2.5 8.242"/><path d="M16 14v6"/><path d="M8 14v6"/><path d="M12 16v6"/></svg>',
  zap: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon"><path d="M4 14a1 1 0 0 1-.78-1.63l9.9-10.2a.5.5 0 0 1 .86.46l-1.92 6.02A1 1 0 0 0 13 10h7a1 1 0 0 1 .78 1.63l-9.9 10.2a.5.5 0 0 1-.86-.46l1.92-6.02A1 1 0 0 0 11 14z"/></svg>',
};
---

<BaseLayout
  title="Pool Cleaning & Maintenance in Savannah & Richmond Hill GA | Perfect Pools"
  description="Professional pool cleaning, maintenance, and repair in Richmond Hill, Savannah & Coastal Georgia. Transparent pricing, consistent visits, and real updates after every service. Call (912) 459-0160."
  canonicalPath="/"
  schema={schema}
>
  <!-- ============ HERO ============ -->
  <section class="hero hero--image">
    <img src={`${import.meta.env.BASE_URL}images/hero-pool.png`} alt="" class="hero__bg" loading="eager" />
    <div class="hero__overlay"></div>
    <div class="container">
      <div class="hero__grid">
        <!-- Left: Hero Text — each piece animates in sequentially. -->
        <div>
          <div class="hero__badge reveal-up" style="--reveal-delay: 0ms">
            <strong style="font-size: 1.1em;">#1</strong>
            <span>Pool Service Provider Across Coastal Counties</span>
          </div>

          <h1 class="reveal-up" style="--reveal-delay: 150ms">Pool care you can count on.</h1>
          <p class="reveal-up" style="--reveal-delay: 300ms">Highest-quality service. Experienced technicians. Clear communication and transparent pricing.</p>

          <div class="hero__buttons reveal-up" style="--reveal-delay: 450ms">
            <a href="tel:9124590160" class="btn btn--cta btn--lg">Call (912) 459-0160</a>
          </div>

          <div class="hero__chips reveal-up" style="--reveal-delay: 600ms">
            <span class="hero__chip">
              <Fragment set:html={icons.shield} />
              Licensed &amp; Insured
            </span>
            <span class="hero__chip">
              <Fragment set:html={icons.droplets} />
              500+ Pools Maintained
            </span>
            <span class="hero__chip">
              <Fragment set:html={icons.clock} />
              20+ Years Experience
            </span>
          </div>
        </div>

        <!-- Right: Mobile shows the two original route cards; desktop shows the contact form. -->
        <div class="hero__cards reveal-up" style="--reveal-delay: 600ms">
          <a href={`${import.meta.env.BASE_URL}services/residential-pool-cleaning/`} class="route-card route-card--gradient">
            <div class="route-card__inner">
              <div class="route-card__icon">
                <Fragment set:html={icons.droplets} />
              </div>
              <div>
                <h3>Weekly Pool Care</h3>
                <p>Ongoing maintenance starting from $50 plus chemicals</p>
                <span class="route-card__link">
                  See Plans <Fragment set:html={icons.arrowRight} />
                </span>
              </div>
            </div>
          </a>

          <a href={`${import.meta.env.BASE_URL}service-repair/`} class="route-card route-card--white">
            <div class="route-card__inner">
              <div class="route-card__icon">
                <Fragment set:html={icons.alertCircle} />
              </div>
              <div>
                <h3>Equipment Repairs</h3>
                <p>Need equipment repaired, replaced or installed? We diagnose most issues within 48 hours</p>
                <span class="route-card__link">
                  Schedule Visit <Fragment set:html={icons.arrowRight} />
                </span>
              </div>
            </div>
          </a>
        </div>

        <div class="hero__form-wrap reveal-up" style="--reveal-delay: 600ms">
          <form class="hero-form" data-hero-form data-active-tab="maintenance">
            <div class="hero-form__tabs" role="tablist" aria-label="Service type">
              <button
                type="button"
                class="hero-form__tab is-active"
                role="tab"
                aria-selected="true"
                data-hero-tab="maintenance"
              >
                <Fragment set:html={icons.droplets} />
                <span>Weekly Pool Care</span>
              </button>
              <button
                type="button"
                class="hero-form__tab"
                role="tab"
                aria-selected="false"
                data-hero-tab="repair"
              >
                <Fragment set:html={icons.alertCircle} />
                <span>Equipment Repairs</span>
              </button>
            </div>

            <div class="hero-form__body">
              <p class="hero-form__blurb" data-hero-blurb>
                Ongoing maintenance starting from <strong>$50</strong> plus chemicals
              </p>

              <div class="hero-form__row">
                <input type="text" name="first_name" placeholder="First name" autocomplete="given-name" required class="hero-form__input" />
                <input type="text" name="last_name" placeholder="Last name" autocomplete="family-name" required class="hero-form__input" />
              </div>
              <input type="email" name="email" placeholder="Email" autocomplete="email" required class="hero-form__input" />
              <input type="tel" name="phone" placeholder="Phone" autocomplete="tel" required class="hero-form__input" />
              <button type="submit" class="btn btn--lg btn--block hero-form__submit" data-hero-submit>Get a Quote</button>
            </div>
          </form>
        </div>
      </div>

      <div class="hero__footer reveal-up" style="--reveal-delay: 900ms">
        <a href={`${import.meta.env.BASE_URL}our-approach`}>First time pool owner? Start here &rarr;</a>
      </div>
    </div>
    <!-- Bottom gradient fade: hero fades to the methodology's base color so the transition feels like one continuous atmosphere. -->
    <div class="hero__fade" aria-hidden="true"></div>
  </section>

  <!-- ============ METHODOLOGY ============ -->
  <section class="methodology methodology--water">
    <!-- Top gradient fade: methodology starts from its dark base and reveals the tile pattern — meets the hero's bottom fade in the same color. -->
    <div class="methodology__fade-top" aria-hidden="true"></div>
    <div id="methodology-liquid-mount"></div>
    <div class="container">
      <div class="section-header">
        <h2 class="reveal-words">
          <span style="--word-index: 0">Clear</span>{' '}
          <span style="--word-index: 1">water</span>{' '}
          <span style="--word-index: 2">isn't</span>{' '}
          <span style="--word-index: 3">luck.</span>{' '}
          <span style="--word-index: 4">Here's</span>{' '}
          <span style="--word-index: 5">how</span>{' '}
          <span style="--word-index: 6">we</span>{' '}
          <span style="--word-index: 7">keep</span>{' '}
          <span style="--word-index: 8">it</span>{' '}
          <span style="--word-index: 9">that</span>{' '}
          <span style="--word-index: 10">way.</span>
        </h2>
        <p class="reveal-up" style="--reveal-delay: 900ms">Our methodology separates us from every other pool company</p>
      </div>

      <!-- Mobile-only tab bar showing there are 3 cards. Hidden on tablet+. -->
      <div class="methodology__tabs" role="tablist" aria-label="Method cards">
        <button type="button" class="methodology__tab is-active" data-card-index="0" aria-label="Chemistry principle"></button>
        <button type="button" class="methodology__tab" data-card-index="1" aria-label="Problem detection principle"></button>
        <button type="button" class="methodology__tab" data-card-index="2" aria-label="Visibility principle"></button>
      </div>
      <div class="methodology__grid">
        <!-- Card delays start after the 11 words finish animating (~950ms),
             with 200ms between each card. -->
        <div class="method-card reveal-up" style="--reveal-delay: 1000ms">
          <div class="method-card__icon">
            <Fragment set:html={icons.testTube} />
          </div>
          <h3>We balance the chemistry, not just add chlorine</h3>
          <p>We test and adjust pH, alkalinity, sanitizer levels, and calcium hardness as a system—because one number out of range affects everything else.</p>
        </div>

        <div class="method-card reveal-up" style="--reveal-delay: 1250ms">
          <div class="method-card__icon">
            <Fragment set:html={icons.alertCircle} />
          </div>
          <h3>We catch problems before they cost you money</h3>
          <p>Every visit includes a full equipment check. We monitor pump pressure, filter condition, and water flow to flag issues early.</p>
        </div>

        <div class="method-card reveal-up" style="--reveal-delay: 1500ms">
          <div class="method-card__icon">
            <Fragment set:html={icons.eye} />
          </div>
          <h3>You see everything we do</h3>
          <p>After every visit, you get a service report with chemical readings, what was adjusted, and photos if anything needs attention.</p>
        </div>
      </div>
    </div>
    <!-- Bottom fade: methodology fades to the services-dark color so the transition into the next section is a smooth atmospheric blend. -->
    <div class="methodology__fade-bottom" aria-hidden="true"></div>
  </section>

  <!-- ============ WHAT WE DO (DARK SECTION) ============ -->
  <section class="services-dark" style="margin-top: -1px;">
    <!-- Top gradient fade: solid services-dark at top fading to transparent,
         meets the methodology's bottom fade in the same color for a seamless atmospheric bridge. -->
    <div class="services-dark__fade-top" aria-hidden="true"></div>
    <div class="container">
      <div class="section-header">
        <h2 class="reveal-words">
          <span style="--word-index: 0">What</span>{' '}
          <span style="--word-index: 1">We</span>{' '}
          <span style="--word-index: 2">Do</span>
        </h2>
        <p class="reveal-up" style="--reveal-delay: 300ms">Ongoing maintenance plans and on-demand repairs for residential and commercial pools.</p>
      </div>

      <div class="services-dark__grid">
        <!-- Left Column: Pool Cleaning -->
        <div class="reveal-up" style="--reveal-delay: 600ms">
          <div class="services-dark__col-header">
            <Fragment set:html={icons.droplets} />
            <h3>Pool Cleaning</h3>
          </div>

          <a href={`${import.meta.env.BASE_URL}services/residential-pool-cleaning/`} class="glass-card">
            <div class="glass-card__inner">
              <div class="glass-card__icon">
                <Fragment set:html={icons.home} />
              </div>
              <div style="flex:1;">
                <div class="glass-card__header">
                  <h4>Need regular maintenance?</h4>
                  <span class="glass-card__arrow"><Fragment set:html={icons.arrowRight} /></span>
                </div>
                <p>Weekly and bi-weekly residential service</p>
                <div class="glass-card__tags">
                  <span class="glass-card__tag">Weekly</span>
                  <span class="glass-card__tag">Bi-weekly<span class="tag-badge tag-badge--winter">Winter Only</span></span>
                </div>
              </div>
            </div>
            <p class="glass-card__desc">Full service including chemical balancing, equipment checks, and detailed reports.</p>
          </a>

          <a href={`${import.meta.env.BASE_URL}services/commercial-pool-cleaning/`} class="glass-card">
            <div class="glass-card__inner">
              <div class="glass-card__icon">
                <Fragment set:html={icons.building} />
              </div>
              <div style="flex:1;">
                <div class="glass-card__header">
                  <h4>Managing a commercial property?</h4>
                  <span class="glass-card__arrow"><Fragment set:html={icons.arrowRight} /></span>
                </div>
                <p>Hotels, apartments, HOAs & public facilities</p>
                <div class="glass-card__tags">
                  <span class="glass-card__tag">Custom Schedule</span>
                </div>
              </div>
            </div>
            <p class="glass-card__desc">High-traffic pool programs with compliance documentation and proactive maintenance.</p>
          </a>

          <button type="button" class="btn btn--cta btn--lg btn--block" style="margin-top:1rem;" data-open-intake>See Plans & Pricing</button>
        </div>

        <!-- Right Column: Equipment & Repair -->
        <div class="reveal-up" style="--reveal-delay: 900ms">
          <div class="services-dark__col-header">
            <Fragment set:html={icons.settings} />
            <h3>Equipment & Repair</h3>
          </div>

          <a href={`${import.meta.env.BASE_URL}service-repair/`} class="glass-card glass-card--sm">
            <div class="glass-card__inner">
              <div class="glass-card__icon glass-card__icon--sm">
                <Fragment set:html={icons.thermometer} />
              </div>
              <div style="flex:1;">
                <div class="glass-card__header">
                  <h4>Pool not heating?</h4>
                  <span class="glass-card__arrow"><Fragment set:html={icons.arrowRight} /></span>
                </div>
                <p>Heat pumps, gas heaters, solar systems</p>
              </div>
            </div>
          </a>

          <a href={`${import.meta.env.BASE_URL}service-repair/`} class="glass-card glass-card--sm">
            <div class="glass-card__inner">
              <div class="glass-card__icon glass-card__icon--sm">
                <Fragment set:html={icons.settings} />
              </div>
              <div style="flex:1;">
                <div class="glass-card__header">
                  <h4>Equipment making noise?</h4>
                  <span class="glass-card__arrow"><Fragment set:html={icons.arrowRight} /></span>
                </div>
                <p>Pumps, motors, filtration diagnosis</p>
              </div>
            </div>
          </a>

          <a href={`${import.meta.env.BASE_URL}service-repair/`} class="glass-card glass-card--sm">
            <div class="glass-card__inner">
              <div class="glass-card__icon glass-card__icon--sm">
                <Fragment set:html={icons.cloudRain} />
              </div>
              <div style="flex:1;">
                <div class="glass-card__header">
                  <h4>Water won't clear up?</h4>
                  <span class="glass-card__arrow"><Fragment set:html={icons.arrowRight} /></span>
                </div>
                <p>Chemistry, filtration, circulation issues</p>
              </div>
            </div>
          </a>

          <a href={`${import.meta.env.BASE_URL}service-repair/`} class="glass-card glass-card--sm">
            <div class="glass-card__inner">
              <div class="glass-card__icon glass-card__icon--sm">
                <Fragment set:html={icons.zap} />
              </div>
              <div style="flex:1;">
                <div class="glass-card__header">
                  <h4>Ready for an upgrade?</h4>
                  <span class="glass-card__arrow"><Fragment set:html={icons.arrowRight} /></span>
                </div>
                <p>Variable-speed pumps, automation, LED lighting</p>
              </div>
            </div>
          </a>

          <button type="button" class="btn btn--outline-white btn--lg btn--block" style="margin-top:1rem;" data-open-intake>Schedule a Diagnosis</button>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ WAVE: What We Do -> Partner ============ -->
  <div class="coastal-wave coastal-wave--bottom" style="background: white;">
    <svg viewBox="0 0 1440 120" preserveAspectRatio="none" fill="none">
      <path d="M0 120L60 105C120 90 240 60 360 45C480 30 600 30 720 37.5C840 45 960 60 1080 60C1200 60 1320 45 1380 37.5L1440 30V120H0Z" fill="#1E3A5F"/>
    </svg>
  </div>

  <!-- ============ A TRUE SERVICE PARTNER ============ -->
  <section class="partner-section">
    <div class="container max-w-4xl mx-auto text-center">
      <h2 class="reveal-up">Complete Pool Care. Built on Local Trust.</h2>

      <p class="reveal-up" style="--reveal-delay: 150ms">We brought together the best local pool professionals in coastal Georgia — and gave them the resources to do even more. From weekly maintenance to full equipment service, one team that knows your pool inside and out.</p>

      <p class="reveal-up" style="--reveal-delay: 300ms">The result is a dedicated service company with the technical depth to handle equipment, the local presence to know your pool, and the operational foundation to deliver consistent care year after year.</p>

      <p class="highlight reveal-up" style="--reveal-delay: 450ms">Your pool is a 15–20 year asset. It deserves a true service partner.</p>

      <div class="reveal-up" style="--reveal-delay: 600ms; margin-top:2rem;">
        <a href={`${import.meta.env.BASE_URL}our-approach/#our-story`} class="btn btn--primary btn--lg">Read Our Story</a>
      </div>
    </div>
  </section>

  <!-- ============ WAVE: Partner -> Coastal ============ -->
  <div class="coastal-wave coastal-wave--top" style="background: white;">
    <svg viewBox="0 0 1440 120" preserveAspectRatio="none" fill="none">
      <path d="M0 120L60 105C120 90 240 60 360 45C480 30 600 30 720 37.5C840 45 960 60 1080 60C1200 60 1320 45 1380 37.5L1440 30V120H0Z" fill="#1E3A5F"/>
    </svg>
  </div>

  <!-- ============ ENGINEERED FOR THE COAST (replaces Markets We Serve) ============ -->
  <section class="coastal-section" id="service-area">
    <div class="container">
      <div class="coastal-layout">
        <!-- Left: Interactive Google Map -->
        <div class="coastal-map reveal-up">
          <div class="coastal-map__glow"></div>
          <div class="coastal-map__frame coastal-map__frame--map">
            <div class="service-area__map" id="service-area-map"></div>
          </div>
        </div>

        <!-- Right: Content -->
        <div class="coastal-content">
          <h2 class="coastal-content__title reveal-up" style="--reveal-delay: 150ms">
            Engineered for<br/>the Coast
          </h2>
          <p class="coastal-content__desc reveal-up" style="--reveal-delay: 300ms">
            Local environments demand local expertise. We've built our methodology
            specifically to combat the unique challenges of coastal pool ownership.
          </p>

          <!-- Address Checker -->
          <div class="coastal-checker reveal-up" style="--reveal-delay: 450ms">
            <h3 class="coastal-checker__title">Check Your Address</h3>
            <p class="coastal-checker__desc">Enter your address to see if you're within our service area.</p>
            <div id="service-area-checker"></div>
          </div>

          <!-- Checklist removed -->
          </ul>
        </div>
      </div>

      <!-- Communities We Serve city cards -->
      <div class="coastal-areas reveal-up">
        <h3 class="coastal-areas__title">Communities We Serve</h3>
        <div class="coastal-areas__track-wrapper">
          <div class="coastal-areas__track" id="area-track">
            <a href={`${import.meta.env.BASE_URL}service-areas/richmond-hill/`} class="coastal-area-card">
              <img src={`${import.meta.env.BASE_URL}images/city-richmond-hill.jpg`} alt="Richmond Hill, Georgia" class="coastal-area-card__img" />
              <div class="coastal-area-card__overlay"></div>
              <div class="coastal-area-card__content">
                <span class="coastal-area-card__label">GA</span>
                <span class="coastal-area-card__name">Richmond Hill</span>
              </div>
            </a>
            <a href={`${import.meta.env.BASE_URL}service-areas/savannah/`} class="coastal-area-card">
              <img src={`${import.meta.env.BASE_URL}images/city-savannah.jpg`} alt="Savannah, Georgia" class="coastal-area-card__img" />
              <div class="coastal-area-card__overlay"></div>
              <div class="coastal-area-card__content">
                <span class="coastal-area-card__label">GA</span>
                <span class="coastal-area-card__name">Savannah</span>
              </div>
            </a>
            <a href={`${import.meta.env.BASE_URL}service-areas/pooler/`} class="coastal-area-card">
              <img src={`${import.meta.env.BASE_URL}images/city-pooler.webp`} alt="Pooler, Georgia" class="coastal-area-card__img" />
              <div class="coastal-area-card__overlay"></div>
              <div class="coastal-area-card__content">
                <span class="coastal-area-card__label">GA</span>
                <span class="coastal-area-card__name">Pooler</span>
              </div>
            </a>
            <a href={`${import.meta.env.BASE_URL}service-areas/hinesville/`} class="coastal-area-card">
              <img src={`${import.meta.env.BASE_URL}images/city-hinesville.jpeg`} alt="Hinesville, Georgia" class="coastal-area-card__img" />
              <div class="coastal-area-card__overlay"></div>
              <div class="coastal-area-card__content">
                <span class="coastal-area-card__label">GA</span>
                <span class="coastal-area-card__name">Hinesville</span>
              </div>
            </a>
            <a href={`${import.meta.env.BASE_URL}service-areas/tybee-island/`} class="coastal-area-card">
              <img src={`${import.meta.env.BASE_URL}images/city-tybee-island.webp`} alt="Tybee Island, Georgia" class="coastal-area-card__img" />
              <div class="coastal-area-card__overlay"></div>
              <div class="coastal-area-card__content">
                <span class="coastal-area-card__label">GA</span>
                <span class="coastal-area-card__name">Tybee Island</span>
              </div>
            </a>
            <a href={`${import.meta.env.BASE_URL}service-areas/bluffton/`} class="coastal-area-card">
              <img src={`${import.meta.env.BASE_URL}images/city-bluffton.webp`} alt="Bluffton, South Carolina" class="coastal-area-card__img" />
              <div class="coastal-area-card__overlay"></div>
              <div class="coastal-area-card__content">
                <span class="coastal-area-card__label">SC</span>
                <span class="coastal-area-card__name">Bluffton</span>
              </div>
            </a>
          </div>
        </div>
        <div class="coastal-areas__nav">
          <button class="coastal-areas__btn" id="cities-prev" aria-label="Previous cities">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M19 12H5M12 19l-7-7 7-7"/></svg>
          </button>
          <button class="coastal-areas__btn" id="cities-next" aria-label="Next cities">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
          </button>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ WAVE: Coastal -> Reviews ============ -->
  <div class="coastal-wave coastal-wave--bottom" style="background: #F9F6F0;">
    <svg viewBox="0 0 1440 120" preserveAspectRatio="none" fill="none">
      <path d="M0 120L60 105C120 90 240 60 360 45C480 30 600 30 720 37.5C840 45 960 60 1080 60C1200 60 1320 45 1380 37.5L1440 30V120H0Z" fill="#1E3A5F"/>
    </svg>
  </div>

  <!-- ============ SOCIAL PROOF ============ -->
  <section class="reviews-home">
    <div class="container">
      <div class="section-header reveal-up">
        <h2>What our customers say</h2>
      </div>
      <div class="reveal-up" style="--reveal-delay: 200ms">
        <ReviewCarousel />
      </div>
      <div class="text-center reveal-up" style="--reveal-delay: 400ms; margin-top:2rem;">
        <a href="https://g.page/r/CVrU0W_3-NRyEAE/review" target="_blank" rel="noopener" class="btn btn--outline">Read All Reviews on Google</a>
      </div>
    </div>
  </section>

  <!-- ============ FINAL CTA WITH IMAGE ============ -->
  <section class="cta-image cta-image--wave">
    <!-- Wave at top — inside section so bg image fills behind it -->
    <div class="cta-image__wave-top">
      <svg viewBox="0 0 1440 120" preserveAspectRatio="none" fill="none">
        <path d="M0 0H1440V30L1380 37.5C1320 45 1200 60 1080 60C960 60 840 45 720 37.5C600 30 480 30 360 45C240 60 120 90 60 105L0 120V0Z" fill="#F9F6F0"/>
      </svg>
    </div>
    <img src={`${import.meta.env.BASE_URL}images/coastal-pool.png`} alt="" class="cta-image__bg" loading="lazy" />
    <div class="cta-image__overlay"></div>
    <div class="container cta-image__content">
      <h2 class="reveal-up">Ready for worry-free pool care?</h2>
      <div class="cta-image__buttons reveal-up" style="--reveal-delay: 200ms">
        <button type="button" class="btn btn--cta btn--lg" data-open-intake>Get a Quote</button>
        <a href="tel:9124590160" class="btn btn--outline-white btn--lg">Call (912) 459-0160</a>
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  import { render, h } from 'preact';
  import ServiceAreaChecker from '../components/ServiceAreaChecker.tsx';
  import LiquidHero from '../components/LiquidHero.tsx';

  const checkerMount = document.getElementById('service-area-checker');
  if (checkerMount) render(h(ServiceAreaChecker, null), checkerMount);

  const liquidMount = document.getElementById('methodology-liquid-mount');
  if (liquidMount) {
    // On mobile we use a clean white-gradient base for the liquid shader
    // (no tile pattern). On desktop we keep the pool-tile photo background.
    const isMobile = window.matchMedia('(max-width: 767px)').matches;
    render(h(LiquidHero, { showText: false, whiteBackground: isMobile }), liquidMount);
  }

  // Render Google Map in the map container
  async function initMap() {
    const mapEl = document.getElementById('service-area-map');
    if (!mapEl || !(window as any).google?.maps) return;
    const google = (window as any).google;
    const center = { lat: 31.6, lng: -81.35 };
    const map = new google.maps.Map(mapEl, {
      center, zoom: 9,
      disableDefaultUI: true,
      zoomControl: true,
      styles: [
        { featureType: 'water', elementType: 'geometry.fill', stylers: [{ color: '#c8e3f5' }] },
        { featureType: 'water', elementType: 'labels', stylers: [{ visibility: 'off' }] },
        { featureType: 'landscape', elementType: 'geometry.fill', stylers: [{ color: '#eef2ee' }] },
        { featureType: 'landscape.natural', elementType: 'geometry.fill', stylers: [{ color: '#e8efe8' }] },
        { featureType: 'road.highway', elementType: 'geometry', stylers: [{ color: '#dde4dd' }, { visibility: 'simplified' }] },
        { featureType: 'road.highway', elementType: 'labels', stylers: [{ visibility: 'off' }] },
        { featureType: 'road.arterial', elementType: 'geometry', stylers: [{ color: '#e8ece8' }, { visibility: 'simplified' }] },
        { featureType: 'road.arterial', elementType: 'labels', stylers: [{ visibility: 'off' }] },
        { featureType: 'road.local', elementType: 'geometry', stylers: [{ color: '#f0f4f0' }, { visibility: 'simplified' }] },
        { featureType: 'road.local', elementType: 'labels', stylers: [{ visibility: 'off' }] },
        { featureType: 'poi', stylers: [{ visibility: 'off' }] },
        { featureType: 'transit', stylers: [{ visibility: 'off' }] },
        { featureType: 'administrative.locality', elementType: 'labels.text.fill', stylers: [{ color: '#4a5568' }] },
        { featureType: 'administrative.locality', elementType: 'labels.text.stroke', stylers: [{ color: '#ffffff' }, { weight: 3 }] },
        { featureType: 'administrative.neighborhood', stylers: [{ visibility: 'off' }] },
      ],
    });
    // Fetch heatmap data from Supabase
    const SUPABASE_URL = 'https://vvprodiuwraceabviyes.supabase.co';
    const SUPABASE_ANON = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZ2cHJvZGl1d3JhY2VhYnZpeWVzIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTY5MDU4MTYsImV4cCI6MjA3MjQ4MTgxNn0.HOFTUrInpmzhxTJDSp1xKp81noC7jMuTcdxr9JqC3z0';
    try {
      const res = await fetch(`${SUPABASE_URL}/functions/v1/heatmap-data`, {
        headers: { apikey: SUPABASE_ANON },
      });
      if (res.ok) {
        const data = await res.json();
        if (data.points?.length > 0) {
          const points = data.points as { lat: number; lng: number }[];
          const areas = [
            { name: 'Savannah', lat: 32.065, lng: -81.09 },
            { name: 'Pooler', lat: 32.12, lng: -81.25 },
            { name: 'Richmond Hill', lat: 31.94, lng: -81.30 },
            { name: 'Wilmington Is.', lat: 31.99, lng: -80.97 },
            { name: 'Tybee Island', lat: 32.00, lng: -80.85 },
            { name: 'Skidaway Is.', lat: 31.94, lng: -81.06 },
            { name: 'Hinesville', lat: 31.84, lng: -81.60 },
            { name: 'Midway', lat: 31.81, lng: -81.43 },
            { name: 'Brunswick', lat: 31.16, lng: -81.49 },
            { name: 'St. Simons Is.', lat: 31.17, lng: -81.37 },
          ];
          const counts = new Map<string, number>();
          areas.forEach(a => counts.set(a.name, 0));
          points.forEach(p => {
            let nearest = areas[0];
            let minD = Infinity;
            areas.forEach(a => {
              const d = (p.lat - a.lat) ** 2 + (p.lng - a.lng) ** 2;
              if (d < minD) { minD = d; nearest = a; }
            });
            counts.set(nearest.name, (counts.get(nearest.name) || 0) + 1);
          });
          areas.forEach(area => {
            const count = counts.get(area.name) || 0;
            if (count < 3) return;
            const scale = Math.min(count / 80, 1);
            new google.maps.Circle({ center: { lat: area.lat, lng: area.lng }, radius: 5000 + scale * 9000, map, fillColor: '#22d3ee', fillOpacity: 0.08 + scale * 0.04, strokeWeight: 0, clickable: false, zIndex: 0 });
            new google.maps.Circle({ center: { lat: area.lat, lng: area.lng }, radius: 3000 + scale * 5500, map, fillColor: '#06b6d4', fillOpacity: 0.12 + scale * 0.06, strokeWeight: 0, clickable: false, zIndex: 1 });
            new google.maps.Circle({ center: { lat: area.lat, lng: area.lng }, radius: 1200 + scale * 2800, map, fillColor: '#0891b2', fillOpacity: 0.17 + scale * 0.09, strokeWeight: 0, clickable: false, zIndex: 2 });
          });
        }
      }
    } catch {};

    // Listen for address check — smooth zoom in and drop pin
    let marker: any = null;
    window.addEventListener('address-checked', ((e: CustomEvent) => {
      const { lat, lng, inArea } = e.detail;
      if (marker) marker.setMap(null);
      const target = { lat, lng };

      // Step 1: Smooth pan to the area
      map.panTo(target);

      // Step 2: Gradually zoom in over time
      const startZoom = map.getZoom() || 9;
      const endZoom = 13;
      let currentZoom = startZoom;
      const zoomStep = () => {
        if (currentZoom >= endZoom) {
          // Step 3: Drop pin after zoom completes
          marker = new google.maps.Marker({
            position: target,
            map,
            icon: {
              path: google.maps.SymbolPath.CIRCLE,
              scale: 10,
              fillColor: inArea ? '#059669' : '#d97706',
              fillOpacity: 1,
              strokeColor: '#fff',
              strokeWeight: 3,
            },
            animation: google.maps.Animation.DROP,
          });
          return;
        }
        currentZoom += 0.5;
        map.setZoom(Math.round(currentZoom));
        setTimeout(zoomStep, 120);
      };
      // Start zooming after pan settles
      setTimeout(zoomStep, 400);
    }) as EventListener);
  }
  // Wait for Google Maps to fully load (Map constructor available)
  const checkMaps = setInterval(() => {
    if ((window as any).google?.maps?.Map) {
      clearInterval(checkMaps);
      // Small delay to ensure DOM is settled
      setTimeout(initMap, 100);
    }
  }, 200);

  // City cards carousel (coastal-areas style)
  const citiesTrack = document.getElementById('area-track') as HTMLElement;
  const citiesPrev = document.getElementById('cities-prev');
  const citiesNext = document.getElementById('cities-next');
  if (citiesTrack && citiesPrev && citiesNext) {
    let cityOffset = 0;
    const getCardWidth = () => {
      const card = citiesTrack.querySelector('.coastal-area-card') as HTMLElement;
      return card ? card.offsetWidth + 16 : 0;
    };
    const getMax = () => Math.max(0, citiesTrack.scrollWidth - (citiesTrack.parentElement as HTMLElement).offsetWidth);
    citiesPrev.addEventListener('click', () => {
      cityOffset = Math.max(0, cityOffset - getCardWidth());
      citiesTrack.style.transform = `translateX(-${cityOffset}px)`;
    });
    citiesNext.addEventListener('click', () => {
      cityOffset = Math.min(getMax(), cityOffset + getCardWidth());
      citiesTrack.style.transform = `translateX(-${cityOffset}px)`;
    });
  }

  // ===== Scroll-triggered reveal =====
  // Elements with .reveal-up or .reveal-words fade/slide in when they enter
  // the viewport. Once revealed they stay revealed (unobserve after first intersection).
  if (!window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    const revealEls = document.querySelectorAll('.reveal-up, .reveal-words');
    if (revealEls.length && 'IntersectionObserver' in window) {
      const io = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
            io.unobserve(entry.target);
          }
        });
      }, { rootMargin: '0px 0px -10% 0px', threshold: 0.15 });
      revealEls.forEach((el) => io.observe(el));
    } else {
      revealEls.forEach((el) => el.classList.add('is-visible'));
    }
  } else {
    document.querySelectorAll('.reveal-up, .reveal-words').forEach((el) => el.classList.add('is-visible'));
  }

  // ===== Hero contact form tabs =====
  // Switches between "Weekly Pool Care" and "Equipment Repairs" tabs.
  // Updates: active tab class, data-active-tab (drives blue/white theming in CSS),
  // blurb text, and submit button label. On submit, opens the intake modal.
  const heroForm = document.querySelector('[data-hero-form]') as HTMLFormElement | null;
  if (heroForm) {
    const heroTabs = heroForm.querySelectorAll<HTMLButtonElement>('[data-hero-tab]');
    const heroSubmit = heroForm.querySelector<HTMLButtonElement>('[data-hero-submit]');
    const heroBlurb = heroForm.querySelector<HTMLElement>('[data-hero-blurb]');
    const blurbs = {
      maintenance: `Ongoing maintenance starting from <strong>$50</strong> plus chemicals`,
      repair: `Need equipment repaired, replaced or installed? We diagnose most issues within <strong>48 hours</strong>.`,
    };
    const updateHeroTab = (type: 'maintenance' | 'repair') => {
      heroForm.dataset.activeTab = type;
      heroTabs.forEach((t) => {
        const isActive = t.dataset.heroTab === type;
        t.classList.toggle('is-active', isActive);
        t.setAttribute('aria-selected', isActive ? 'true' : 'false');
      });
      if (heroSubmit) {
        heroSubmit.textContent = type === 'maintenance' ? 'Get a Quote' : 'Request a Call';
      }
      if (heroBlurb) {
        heroBlurb.innerHTML = blurbs[type];
      }
    };
    heroTabs.forEach((tab) => {
      tab.addEventListener('click', () => {
        const type = tab.dataset.heroTab as 'maintenance' | 'repair';
        updateHeroTab(type);
      });
    });
    heroForm.addEventListener('submit', (e) => {
      e.preventDefault();
      const intakeTrigger = document.querySelector('[data-open-intake]') as HTMLButtonElement | null;
      if (intakeTrigger) intakeTrigger.click();
    });
  }

  // ===== Methodology mobile carousel + tabs =====
  // On mobile the method-cards become a horizontal scroll-snap carousel.
  // Tabs sync with the active card, and clicking a tab scrolls to that card.
  const methGrid = document.querySelector('.methodology__grid') as HTMLElement | null;
  const methTabs = document.querySelectorAll<HTMLButtonElement>('.methodology__tab');
  if (methGrid && methTabs.length > 0) {
    const methCards = methGrid.querySelectorAll<HTMLElement>('.method-card');
    methTabs.forEach((tab, i) => {
      tab.addEventListener('click', () => {
        const card = methCards[i];
        if (!card) return;
        methGrid.scrollTo({ left: card.offsetLeft - (methGrid.clientWidth - card.offsetWidth) / 2, behavior: 'smooth' });
      });
    });
    let methTicking = false;
    const updateMethTabs = () => {
      const center = methGrid.scrollLeft + methGrid.clientWidth / 2;
      let activeIdx = 0;
      let minDist = Infinity;
      methCards.forEach((c, i) => {
        const dist = Math.abs((c.offsetLeft + c.offsetWidth / 2) - center);
        if (dist < minDist) { minDist = dist; activeIdx = i; }
      });
      methTabs.forEach((t, i) => t.classList.toggle('is-active', i === activeIdx));
      methTicking = false;
    };
    methGrid.addEventListener('scroll', () => {
      if (!methTicking) {
        requestAnimationFrame(updateMethTabs);
        methTicking = true;
      }
    }, { passive: true });
    updateMethTabs();
  }

  // ===== Hero exit progress (scroll-linked) =====
  // Exposes --hero-exit (0..1) on the hero element as the user scrolls through it.
  // CSS uses this for parallax on the background image, content exit translation,
  // and the fade overlay's intensity.
  const heroExitEl = document.querySelector('.hero--image') as HTMLElement | null;
  if (heroExitEl && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    let heroTicking = false;
    const updateHeroExit = () => {
      const rect = heroExitEl.getBoundingClientRect();
      const progress = Math.max(0, Math.min(1, -rect.top / rect.height));
      heroExitEl.style.setProperty('--hero-exit', progress.toString());
      heroTicking = false;
    };
    const onHeroScroll = () => {
      if (!heroTicking) {
        requestAnimationFrame(updateHeroExit);
        heroTicking = true;
      }
    };
    updateHeroExit();
    window.addEventListener('scroll', onHeroScroll, { passive: true });
    window.addEventListener('resize', onHeroScroll, { passive: true });
  }
</script>

<style>
  /* ========================================
     Wave Separators
     ======================================== */
  .coastal-wave {
    position: relative;
    z-index: 2;
    line-height: 0;
  }

  .coastal-wave svg {
    width: 100%;
    height: 64px;
    display: block;
  }

  .coastal-wave--top {
    margin-bottom: -1px;
  }

  .coastal-wave--bottom {
    margin-top: -1px;
    transform: rotate(180deg);
  }

  @media (min-width: 900px) {
    .coastal-wave svg {
      height: 96px;
    }
  }

  @media (min-width: 1100px) {
    .coastal-wave svg {
      height: 120px;
    }
  }

  /* ========================================
     Coastal Expertise Section
     ======================================== */
  .coastal-section {
    background: #1E3A5F;
    color: #fff;
    padding: var(--space-16) 0;
    position: relative;
    z-index: 1;
    overflow: hidden;
  }

  .coastal-layout {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3rem;
  }

  @media (min-width: 900px) {
    .coastal-layout {
      flex-direction: row;
      gap: 4rem;
    }
  }

  /* Map graphic container */
  .coastal-map {
    flex: 0 0 auto;
    width: 100%;
    max-width: 400px;
    position: relative;
  }

  @media (min-width: 900px) {
    .coastal-map {
      flex: 0 0 45%;
      max-width: none;
    }
  }

  .coastal-map__glow {
    position: absolute;
    inset: -10%;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 50%;
    filter: blur(40px);
  }

  .coastal-map__frame {
    position: relative;
    width: 100%;
    aspect-ratio: 1;
    border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
    overflow: hidden;
    border: 3px solid rgba(255, 255, 255, 0.12);
    background: rgba(30, 40, 60, 0.5);
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
  }

  /* Override for the interactive map — organic blob shape like our-approach page */
  .coastal-map__frame--map {
    border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
    aspect-ratio: 1;
    border: 3px solid rgba(255, 255, 255, 0.12);
    background: rgba(30, 40, 60, 0.5);
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
  }

  .coastal-map__frame--map .service-area__map {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    border-radius: inherit;
  }

  /* Content column */
  .coastal-content {
    flex: 1;
  }

  .coastal-content__title {
    font-family: var(--font-heading);
    font-size: clamp(1.75rem, 4vw, 2.75rem);
    font-weight: 700;
    line-height: 1.15;
    letter-spacing: -0.02em;
    color: #fff;
    margin-bottom: 1rem;
  }

  .coastal-content__desc {
    font-size: clamp(0.95rem, 1.5vw, 1.1rem);
    line-height: 1.65;
    color: rgba(226, 232, 240, 0.9);
    max-width: 500px;
    margin-bottom: 2rem;
  }

  /* Address checker on dark bg */
  .coastal-checker {
    margin-bottom: 2rem;
    padding: 1.5rem;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-lg);
  }

  .coastal-checker__title {
    font-family: var(--font-heading);
    font-size: 1.15rem;
    font-weight: 600;
    color: #fff;
    margin-bottom: 0.35rem;
  }

  .coastal-checker__desc {
    font-size: 0.85rem;
    color: rgba(203, 213, 225, 0.85);
    margin-bottom: 1rem;
    line-height: 1.5;
  }

  /* Style the address checker inputs on dark bg */
  .coastal-checker :global(input),
  .coastal-checker :global(input[type="text"]) {
    background: rgba(255, 255, 255, 0.1) !important;
    border: 1px solid rgba(255, 255, 255, 0.2) !important;
    color: #fff !important;
    border-radius: 8px;
  }

  .coastal-checker :global(input::placeholder) {
    color: rgba(203, 213, 225, 0.6) !important;
  }

  .coastal-checker :global(input:focus) {
    border-color: #E28D33 !important;
    box-shadow: 0 0 0 3px rgba(226, 141, 51, 0.2) !important;
  }

  /* Coastal checklist */
  .coastal-checklist {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .coastal-checklist__item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
  }

  .coastal-checklist__icon {
    flex-shrink: 0;
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: rgba(226, 141, 51, 0.2);
    color: #E28D33;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: 2px;
    transition: background 200ms ease, color 200ms ease;
  }

  @media (hover: hover) and (pointer: fine) {
    .coastal-checklist__item:hover .coastal-checklist__icon {
      background: #E28D33;
      color: #fff;
    }
  }

  .coastal-checklist__item strong {
    display: block;
    font-size: 1.05rem;
    font-weight: 600;
    color: #fff;
    margin-bottom: 0.2rem;
  }

  .coastal-checklist__item span {
    display: block;
    font-size: 0.85rem;
    color: rgba(203, 213, 225, 0.85);
    line-height: 1.5;
  }

  /* ========================================
     Communities We Serve — City Cards Carousel
     ======================================== */
  .coastal-areas {
    margin-top: 3.5rem;
  }

  .coastal-areas__title {
    font-family: var(--font-heading);
    font-size: clamp(1.25rem, 2.5vw, 1.5rem);
    font-weight: 700;
    color: #fff;
    margin-bottom: 1.25rem;
  }

  .coastal-areas__nav {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    margin-top: 1.5rem;
  }

  .coastal-areas__btn {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.15);
    color: rgba(255, 255, 255, 0.7);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: background 200ms ease, color 200ms ease, border-color 200ms ease;
  }

  @media (hover: hover) and (pointer: fine) {
    .coastal-areas__btn:hover {
      background: rgba(255, 255, 255, 0.18);
      color: #fff;
      border-color: rgba(226, 141, 51, 0.5);
    }
  }

  .coastal-areas__track-wrapper {
    overflow: hidden;
  }

  .coastal-areas__track {
    display: flex;
    gap: 1rem;
    transition: transform 300ms cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .coastal-area-card {
    flex: 0 0 calc(50% - 0.5rem);
    position: relative;
    aspect-ratio: 4 / 3;
    border-radius: var(--radius-lg);
    overflow: hidden;
    text-decoration: none;
    color: #fff;
    cursor: pointer;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  @media (min-width: 600px) {
    .coastal-area-card {
      flex: 0 0 calc(33.333% - 0.67rem);
    }
  }

  @media (min-width: 900px) {
    .coastal-area-card {
      flex: 0 0 calc(25% - 0.75rem);
    }
  }

  @media (hover: hover) and (pointer: fine) {
    .coastal-area-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 16px 40px rgba(0, 0, 0, 0.35);
      color: #fff;
    }
  }

  .coastal-area-card__img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 400ms ease-out;
  }

  @media (hover: hover) and (pointer: fine) {
    .coastal-area-card:hover .coastal-area-card__img {
      transform: scale(1.06);
    }
  }

  .coastal-area-card__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(0, 0, 0, 0.7) 0%, rgba(0, 0, 0, 0.1) 50%, transparent 100%);
  }

  .coastal-area-card__content {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .coastal-area-card__label {
    font-size: 0.65rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #E28D33;
  }

  .coastal-area-card__name {
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: 1.25rem;
    line-height: 1.2;
    color: #fff;
  }
</style>
